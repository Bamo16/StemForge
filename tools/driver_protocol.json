{
  "note": "Single source of truth for the separator_driver.py wire protocol vocabulary. separator_driver.py loads this and asserts every emitted event/phase is declared here; the C# DriverEvent records and DriverPhase enum mirror it, and a contract test fails the build on drift. Adding or renaming an event or phase requires editing this file plus both sides. See docs/adr/0008.",
  "events": [
    "ready",
    "presets",
    "phase",
    "progress",
    "log",
    "stem_written",
    "job_completed",
    "job_failed",
    "job_cancelled",
    "error",
    "bye"
  ],
  "phases": [
    "downloading_model",
    "loading_model",
    "separating",
    "ensembling"
  ]
}
